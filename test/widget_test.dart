import 'package:flutter_test/flutter_test.dart';

void main() {
  test('darkMode geht ein', () {
    // final appstateProvider = globalRef?.read(
    //   refAppState.notifier,
    // );

    // expect(appstateProvider?.state.darkMode, false);
    // appstateProvider?.switchDarkmode();
    // expect(appstateProvider?.state.darkMode, true);
  });
}
